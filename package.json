{
  "name": "cinesuggest",
  "version": "1.0.0",
  "scripts": {
    "build": "cd cs-cards && npm install && npm run build && cd ../ai-studio-import && npm install && npm run build"
  },
  "dependencies": {
    "3dsvg": "^0.2.1",
    "gsap": "^3.15.0",
    "motion": "^12.42.2"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4"
  }
}
